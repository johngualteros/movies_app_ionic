import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'login_design_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
